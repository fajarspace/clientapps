import Changelog from "../../../components/Changelog/page";
import withDashboardLayout from "../../../components/hoc/withDashboardLayout";

const ChangelogPage = () => {
  return <Changelog />;
};

export default withDashboardLayout(ChangelogPage);
